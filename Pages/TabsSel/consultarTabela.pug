extends ../Templates/layout
block corpo
    #tabela-selecao
        h1.page-header {{name}}
        .row
            h3.sub-header Classes
            .row.placeholders
                custom-table-waterfall(
                    v-if="ready"

                    filter-on="true"
                    pages-on="true"

                    :header="tableHeader"
                    :complete-rows="tableData"
                    :sub-ready="subReady"
                    :cwidth="cwidth"
                    :n-edits="nEdits"
                    
                    table-class='table table-hover'
                    
                    @row-clicked="rowClicked($event)"
                    @drop-clicked="dropClicked($event)"
                )
                h4(
                    v-else
                ) Loading...
        
    //-
append scripts    
    //-script(src="https://cdn.jsdelivr.net/npm/vue-resource@1.3.4")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/lodash.js/4.14.1/lodash.min.js")
    script(src="/Scripts/TabsSel/consulta.vue")