type: array
items:
  type: object
  properties:
    _id:
      type: object
      properties:
        $oid:
          type: string
      required:
        - $oid
    classe:
      type: string
    entradas:
      $ref: '#/components/schemas/DocEntradas'
  required:
    - _id
    - classe
    - entradas
