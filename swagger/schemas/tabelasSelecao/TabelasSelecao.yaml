type: array
items:
  type: object
  properties:
    id:
      $ref: '#/components/schemas/TSIdCompleto'
    designacao:
      type: string
    data:
      $ref: '#/components/schemas/Data'
    entidades:
      type: array
      items:
        $ref: '#/components/schemas/EntidadeIdCompleto'
  required:
    - id
    - designacao
    - data
    - entidades
