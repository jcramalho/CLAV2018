type: array
items:
  type: object
  properties:
    id:
      $ref: '#/components/schemas/AutoEliminacaoIdCompleto'
    data:
      $ref: '#/components/schemas/Data'
    entidade:
      $ref: '#/components/schemas/EntidadeIdCompleto'
    numero:
      $ref: '#/components/schemas/NumeroLeg'
    tipo:
      $ref: '#/components/schemas/Fonte'
    referencial:
      $ref: '#/components/schemas/TSIdCompleto'
    referencialLabel:
      type: string
    referencialTitulo:
      type: string
  required:
    - id
    - data
    - entidade
    - numero
    - tipo
