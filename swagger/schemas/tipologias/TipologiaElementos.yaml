type: array
items:
  type: object
  properties:
    designacao:
      type: string
    sigla:
      type: string
    id:
      $ref: '#/components/schemas/EntidadeId'
  required:
    - designacao
    - sigla
    - id
