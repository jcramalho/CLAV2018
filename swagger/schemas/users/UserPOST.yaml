type: object
properties:
  email:
    type: string
    format: email
  entidade:
    $ref: '#/components/schemas/EntidadeId'
  type:
    $ref: '#/components/schemas/UserLevel'
  name:
    type: string
  password:
    type: string
required:
  - email
  - entidade
  - type
  - name
  - password
