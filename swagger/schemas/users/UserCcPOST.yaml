type: object
properties:
  email:
    type: string
    format: email
  entidade:
    $ref: '#/components/schemas/EntidadeId'
  type:
    $ref: '#/components/schemas/UserLevel'
  name:
    type: string
  nic:
    $ref: '#/components/schemas/NIC'
required:
  - email
  - entidade
  - type
  - name
  - nic
