type: array
items:
  type: object
  properties:
    _id:
      $ref: '#/components/schemas/Rota'
    nCallsGet:
      type: integer
      minimum: 0
    nCallsPost:
      type: integer
      minimum: 0
    nCallsPut:
      type: integer
      minimum: 0
  required:
    - _id
    - nCallsGet
    - nCallsPost
    - nCallsPut
