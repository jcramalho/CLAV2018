type: array
items:
  type: object
  properties:
    classe:
      type: string
    codigo:
      type: string
    classePai:
      type: string
    titulo:
      type: string
    nivel:
      $ref: '#/components/schemas/Nivel'
    descricao:
      type: string
    referencia:
      type: string
    df:
      $ref: '#/components/schemas/DFValor'
    pca:
      $ref: '#/components/schemas/PCAValor'
    notaDF:
      type: string
    notaPCA:
      type: string
    formaContagem:
      $ref: '#/components/schemas/FormaContagem'
    subFormaContagem:
      type: string
    designacaoDono:
      type: string
    designacaoParticipante:
      type: string
  required:
    - classe
    - codigo
    - titulo
    - nivel
