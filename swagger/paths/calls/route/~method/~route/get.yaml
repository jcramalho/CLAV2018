summary: Devolve os pedidos realizados (log) para uma determinada rota
description: Devolve os pedidos realizados (log) para uma determinada rota. Cada entrada no log tem o seu id (_id), o caminho da rota, o método, o tipo de acesso (User ou Chave), o id do utilizador ou da chave API, o status HTTP do pedido e a altura quando foi realizado o pedido.
tags:
  - calls
security:
  - userQuery: []
  - userAuth: []
parameters:
  - name: method
    in: path
    required: true
    description: Método da rota
    schema:
      type: string
      enum: [GET, POST, PUT, DELETE]
  - name: route
    in: path
    required: true
    description: Caminho da rota
    schema:
      type: string
      example: "/api/tipologias"
responses:
  200:
    description: Pedidos realizados (log)
    content:
      application/json:
        schema:
          type: string
  401:
    $ref: '#/components/responses/UnauthorizedUser'
  403:
    $ref: '#/components/responses/PermissionsUser'
  500:
    description: Erro
