summary: Verifica se uma determinada sigla de uma tipologia já existe
description: Verifica se uma determinada sigla de uma tipologia já existe
tags:
  - tipologias
security:
  - userQuery: []
  - userAuth: []
requestBody:
  description: 'sigla da tipologia a verificar'
  required: true
  content:
    application/json:
      schema:
        properties:
          sigla:
            type: string
        example:
          sigla: AAC
responses:
  200:
    description: Verifica se uma determinada sigla de uma tipologia já existe
  401:
    $ref: '#/components/responses/UnauthorizedUser'
  403:
    $ref: '#/components/responses/PermissionsUser'
  500:
    description: Erro
