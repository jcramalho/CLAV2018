summary: Lista as classes da tabela de seleção
description: Lista as classes da tabela de seleção
security:
  - apiKeyQuery: []
  - apiKeyAuth: []
  - userQuery: []
  - userAuth: []
tags:
  - Tabelas de Seleção
parameters:
  - name: id
    in: path
    description: 'Identificador de uma tabela de seleção'
    schema:
      $ref: '#/components/schemas/TSId'
    required: true
responses:
  200:
    description: Lista as classes da tabela de seleção
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/TabelaSelecaoClasses'
        example: !!inc/file '/examples/tabelasSelecao/TabelaSelecaoClasses.yaml'
  401:
    $ref: '#/components/responses/UnauthorizedKey'
  403:
    $ref: '#/components/responses/PermissionsKey'
  422:
    $ref: '#/components/responses/ValidationParameters'
  500:
    description: Erro
