summary: Devolve a informação base do PCA
description: Devolve a informação base do PCA, ou seja, idPCA, formaContagem, subFormaContagem, idJustificacao, valores e notas
tags:
  - classes
parameters:
  - name: id
    in: path
    description: 'Identificador de uma classe (exemplo: c200.10.100)'
    schema:
      type: string
    required: true
responses:
  200:
    description: Devolve a informação base do PCA
  500:
    description: Erro
