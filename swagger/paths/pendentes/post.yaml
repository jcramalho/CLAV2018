summary: Guarda um trabalho pendente
description: Guarda um trabalho pendente
security:
  - userQuery: []
  - userAuth: []
tags:
  - Pendentes
requestBody:
  description: Pendente a guardar
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/PendentePOST'
      example: !!inc/file '/examples/pendentes/PendentePOST.yaml'
responses:
  200:
    description: Guardado um trabalho pendente
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/PendentePOSTres'
        example: !!inc/file '/examples/pendentes/PendentePOSTres.yaml'
  401:
    $ref: '#/components/responses/UnauthorizedUser'
  403:
    $ref: '#/components/responses/PermissionsUser'
  422:
    $ref: '#/components/responses/ValidationParameters'
  500:
    description: Erro
