summary: Guardar um trabalho pendente
description: Guardar um trabalho pendente
security:
  - userQuery: []
  - userAuth: []
tags:
  - pendentes
requestBody:
  description: 'Dados. No campo objeto é colocado o objeto a guardar. Pode ser um objeto Classe, TS Organizacional, TS Pluriorganizacional, Entidade, Tipologia, Legislação ou Termo de Indice'
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/Pendente'
responses:
  200:
    description: Guardar um trabalho pendente
  401:
    $ref: '#/components/responses/UnauthorizedUser'
  403:
    $ref: '#/components/responses/PermissionsUser'
  500:
    description: Erro
