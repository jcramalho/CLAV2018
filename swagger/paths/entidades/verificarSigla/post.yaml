summary: Verifica se uma determinada sigla de uma entidade já existe
description: Verifica se uma determinada sigla de uma entidade já existe
tags:
  - entidades
security:
  - userQuery: []
  - userAuth: []
requestBody:
  description: 'sigla da entidade a verificar'
  required: true
  content:
    application/json:
      schema:
        properties:
          sigla:
            type: string
        example:
          sigla: DGLAB
responses:
  200:
    description: Verifica se uma determinada sigla de uma entidade já existe
  401:
    $ref: '#/components/responses/UnauthorizedUser'
  403:
    $ref: '#/components/responses/PermissionsUser'
  500:
    description: Erro
