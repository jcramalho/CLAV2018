summary: Criar chave API
description: Criar Chave API
tags:
  - Chaves
requestBody:
  description: Para criar uma chave é necessário o nome para a chave, o email e a entidade
  required: true
  content:
    application/json:
      schema:
        properties:
          name:
            type: string
          email:
            type: string
          entidade:
            type: string
responses:
  200:
    description: Chave API criada
  500:
    description: Erro
