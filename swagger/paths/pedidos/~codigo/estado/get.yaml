summary: Query ao estado do pedido
description: |
  Query ao estado do pedido

  **Acesso permitido a:** Utilizadores com nível 1, 2, 3, 3.5, 4, 5, 6 e 7
security:
  - userQuery: []
  - userAuth: []
tags:
  - Pedidos
parameters:
  - name: codigo
    in: path
    description: Identificador de um pedido
    schema:
      $ref: '#/components/schemas/PedidoCodigo'
    example: 2020-0000010
    required: true
responses:
  200:
    description: Estado do pedido
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/PedidoCodigo'
        example: 2020-0000011
  401:
    $ref: '#/components/responses/UnauthorizedUser'
  403:
    $ref: '#/components/responses/PermissionsUser'
  500:
    description: Erro
