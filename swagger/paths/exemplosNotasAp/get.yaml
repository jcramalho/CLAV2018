summary: Devolve a lista de todos os exemplos de notas de aplicação ou verifica se um determinado exemplo de nota de aplicação existe
description: Devolve a lista de todas os exemplos de notas de aplicação. Para cada exemplo de nota de aplicação devolve idExemplo, exemplo, cProc e tituloProc ou verifica se um determinado exemplo de nota de aplicação existe através do parâmetro 'existe'
tags:
  - diversos
security:
  - apiKeyQuery: []
  - apiKeyAuth: []
  - userQuery: []
  - userAuth: []
parameters:
  - in: query
    name: existe
    schema:
      type: string
    description: 'nome do exemplo de nota de aplicação a verificar se existe (exemplo: Código da Estrada)'
    required: false
responses:
  200:
    description: Devolve a lista de todos os exemplos de notas de aplicação ou verifica se um determinado exemplo de nota de aplicação existe
  401:
    $ref: '#/components/responses/UnauthorizedKey'
  403:
    $ref: '#/components/responses/PermissionsKey'
  500:
    description: Erro
