TODOs:
    Utilizadores
        -controlo de permissões
        -fluxo de submissões
        -autenticação por CC

    API
        -alterar deletes para fazer apenas remoção lógica (status=inativo)
        -separar consultar em /:id (meta e relações) e /:id/base (meta)

    MISC
        -arrumar CSS

    LOGs
        Classe::timestamp::campo(alterado)::user::valor_antigo